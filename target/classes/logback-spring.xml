<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">
    <property name="LOG_PATH" value="./logs" />
    <property name="APP_NAME" value="LOGS" />

    <!-- Console Appender (JSON estructurado) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp />
                <pattern>
                    <pattern>
                        {
                        "level": "%level",
                        "thread": "%thread",
                        "logger": "%logger",
                        "message": "%message",
                        "trace": "%ex{5}",
                        "app": "${APP_NAME}",
                        "env": "${spring.profiles.active:-local}"
                        }
                    </pattern>
                </pattern>
                <mdc /> <!-- Mapped Diagnostic Context (userId, requestId, etc.) -->
            </providers>
        </encoder>
    </appender>

    <!-- File Appender con rotación -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp />
                <pattern>
                    <pattern>
                        {
                        "level": "%level",
                        "thread": "%thread",
                        "logger": "%logger",
                        "message": "%message",
                        "trace": "%ex{5}",
                        "app": "${APP_NAME}",
                        "env": "${spring.profiles.active:-local}"
                        }
                    </pattern>
                </pattern>
                <mdc />
            </providers>
        </encoder>
    </appender>

    <!-- Logstash (para ELK o Loki) -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>localhost:5000</destination> <!-- apunta a Logstash o Loki -->
        <encoder class="net.logstash.logback.encoder.LogstashEncoder" />
    </appender>

    <!-- Niveles -->
    <logger name="org.springframework" level="INFO" />
    <logger name="com.mycompany" level="DEBUG" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="LOGSTASH" />
    </root>
</configuration>
